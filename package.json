{
  "name": "sns-image-rotator",
  "description": "Rotate images on X (Twitter) and Bluesky with one click",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@wxt-dev/auto-icons": "^1.1.1",
    "typescript": "^5.6.0",
    "wxt": "^0.20.0"
  },
  "overrides": {
    "adm-zip": ">=0.6.0",
    "esbuild": ">=0.28.1",
    "shell-quote": ">=1.8.4",
    "tmp": ">=0.2.6",
    "uuid": ">=11.1.1"
  }
}
