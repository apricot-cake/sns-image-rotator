#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Gitleaks is required to commit in this repository." >&2
  echo "Install it from https://github.com/gitleaks/gitleaks#installing, then retry." >&2
  exit 1
fi

exec gitleaks git --pre-commit --redact --staged --verbose
